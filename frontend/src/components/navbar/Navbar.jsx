// local imports
import { ICONS } from '../../constants';

const Navbar = () => {
    return (
        <div>
            <img src={ICONS.logo} alt="logo" />
        </div>
    );
};

export default Navbar;
